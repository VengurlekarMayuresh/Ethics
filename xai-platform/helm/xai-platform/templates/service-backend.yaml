apiVersion: v1
kind: Service
metadata:
  name: {{ include "xai-platform.fullname" . }}-backend
  labels:
    {{- include "xai-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "xai-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
